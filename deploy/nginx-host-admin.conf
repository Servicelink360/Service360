# Host nginx — install to /etc/nginx/sites-available/service360-admin
# Admin: https://admin.service360.com.au/  -> localhost:8080 (docker admin)

server {
    listen 80;
    server_name admin.service360.com.au;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name admin.service360.com.au;

    ssl_certificate     /etc/letsencrypt/live/api.service360.com.au/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.service360.com.au/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
