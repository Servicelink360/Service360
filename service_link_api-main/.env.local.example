# Copy to .env.local (gitignored). Overrides .env for your machine only.
# Required if your local postgres password differs from .env.

DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=YOUR_LOCAL_POSTGRES_PASSWORD
DATABASE_DB_NAME=service360
DATABASE_SSL=false
